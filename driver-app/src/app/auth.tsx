import { ComingSoon } from '@/features/_placeholder/ComingSoon';

// Placeholder until the auth feature (sign-in → OTP → success) lands.
export default function AuthRoute() {
  return <ComingSoon title="Sign in" />;
}
