import { ComingSoon } from '@/features/_placeholder/ComingSoon';

// Placeholder until the trip-history feature (D3) lands.
export default function TripsRoute() {
  return <ComingSoon title="Trips" />;
}
