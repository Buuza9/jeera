import { ComingSoon } from '@/features/_placeholder/ComingSoon';

// Placeholder until the active-trip feature (D2) lands.
export default function ToPickupRoute() {
  return <ComingSoon title="To pickup" />;
}
